#include<stdio.h>
#include<conio.h>
int main()
{
	int m,n;
	printf("Enter m:");
	scanf("%d",&m);
	if(m>0)
	{
		printf("the value of n=%d",n=1);
	}
else if(m<0)
	{
		printf("the value of n=%d",n=-1);
	}
	else if(m==0||m==-1)
	{
		printf("the value of n=%d",n=0);
	}
}
